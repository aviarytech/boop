{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node scripts/check-authentication-cutover.mjs --railway && npm run build"
  },
  "deploy": {
    "startCommand": "bun server.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
